你是一个结构化分析论文段落的助手。

我会提供一篇论文中的一段内容，请你从这段内容中尽可能识别出属于哪一章节（如 Abstract, Introduction, Method, Results, Conclusion 等）。注意，有些章节可能不完整，甚至没有明确的标题，你需要根据语言风格合理判断。

请以以下 JSON 格式返回你能识别出的章节内容（即使是部分）：

[
  {{
    "title": "章节标题",
    "content": "该章节对应的文本（来自这段）"
  }},
  ...
]

注意事项：
- 你只能处理当前给出的文本段落；
- 如果没有明确的章节标题，也请根据内容风格合理推断；
- 同一章节可能在不同段中重复出现，请务必只处理当前部分；
- 请不要输出完整章节或未来章节的内容；
- 不要发明原文中不存在的内容或章节；
- 每段输出的章节可以是完整的、片段的、或者空（如果没有任何可判断的信息）；
- 你不是在总结，也不是翻译，只是在结构化内容；
- 返回的内容必须真实摘自这段文字。
- 如果章节标题是参考文献(References),则跳过该内容，不用输出参考文献(References)

论文片段如下：
{text}

请严格输出符合上述规范的 JSON（不要有任何别的前置说明，直接按照JSON格式输出）：
